name: Deployment
run-name: ${{ github.actor }} is running 🚀
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
jobs:
  build:
    runs-on: node:22-slim
    name: Build
    - uses: actions/checkout@v3
    steps:
      - run: npm install -g @angular/cli
      - run: npm run build
  # test:
  # needs: [test]
  #   name: Testing

  # deploy:
  #   needs: [build, test]
  #   name: Deployment